# Thai Character Recognition — pinned to the environment used for training
# (Apple Silicon / MPS). On CUDA machines install the matching torch build
# from https://pytorch.org instead of the pins below.
torch==2.12.1
torchvision==0.27.1
scikit-learn==1.3.0
matplotlib==3.7.2
seaborn==0.12.2
tqdm==4.65.0
numpy==1.26.4
Pillow==9.4.0
